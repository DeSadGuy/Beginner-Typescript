// classic single linked list
// testing some stuff 
const addUncurried = ([a, b]) => a + b;
const testadd = ([a, b]) => a + b;
// syntax for function 
const sumname = (a, b) => a + b;
const addfull = ([a, b]) => a + b;
//const add = (a: number) => (b: number) => (c: number) => a + b + c;
const addspecial = (a) => (b) => (c) => a + b + c;
let user = {
    name: 'John',
    age: 30
};
//# sourceMappingURL=main.js.map